authorizeWebApi         Authorize ROhdsiWebApi to access a protected
                        instance of WebAPI Authorize the ROhdsiWebApi
                        package to access WebApi on behalf of the user.
                        This can be done with any of the auth methods
                        described below. authorizeWebApi will use
                        attempt to retrieve, cache, and update a token
                        which will grant access to webAPI by all
                        subsequent requests made by the package.
cancelCharacterizationGeneration
                        Cancel generation of Characterization id.
cancelCohortGeneration
                        Cancel generation of Cohort id.
cancelGeneration        Invoke generation. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
cancelIncidenceRateGeneration
                        Cancel generation of IncidenceRate id.
cancelPathwayGeneration
                        Cancel generation of Pathway id.
checkInputFileEncoding
                        Check character encoding of input file
convertConceptSetDefinitionToTable
                        Convert a concept set definition to a table
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
createConceptSetWorkbook
                        Save a set of concept sets expressions,
                        included concepts, and mapped concepts into a
                        workbook \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
deleteCharacterizationDefinition
                        Delete Characterization id definition.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
deleteCohortDefinition
                        Delete Cohort id definition. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
deleteConceptSetDefinition
                        Delete ConceptSet id definition.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
deleteDefinition        Delete a definition id of a chosen category.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
deleteEstimationDefinition
                        Delete Estimation id definition.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
deleteIncidenceRateDefinition
                        Delete IncidenceRate id definition.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
deletePathwayDefinition
                        Delete Pathway id definition.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
deletePredictionDefinition
                        Delete Prediction id definition.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
detectCharacterizationsByName
                        Detect the presence of string matched
                        Characterization definitions.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
detectCohortsByName     Detect the presence of string matched Cohort
                        definitions. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
detectConceptSetsByName
                        Detect the presence of string matched
                        ConceptSet definitions. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
detectEstimationsByName
                        Detect the presence of string matched
                        Estimation definitions. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
detectIncidenceRatesByName
                        Detect the presence of string matched
                        IncidenceRate definitions. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
detectPathwaysByName    Detect the presence of string matched Pathway
                        definitions. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
detectPredictionsByName
                        Detect the presence of string matched
                        Prediction definitions. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
existsCharacterizationName
                        Check if Characterization definition name
                        exists. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
existsCohortName        Check if Cohort definition name exists.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
existsConceptSetName    Check if ConceptSet definition name exists.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
existsEstimationName    Check if Estimation definition name exists.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
existsIncidenceRateName
                        Check if IncidenceRate definition name exists.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
existsPathwayName       Check if Pathway definition name exists.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
existsPredictionName    Check if Prediction definition name exists.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
exportCohortDefinitionSet
                        Export cohort definition set from WebAPI
getCdmSources           Get the data sources in the WebAPI instance
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getCharacterizationDefinition
                        Get Characterization id definition.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getCharacterizationDefinitionsMetaData
                        Get the meta data for Characterization
                        definitions. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getCharacterizationGenerationInformation
                        Get generation information for Characterization
                        id.
getCharacterizationResults
                        Get results for a Characterization Id.
getCohortDefinition     Get Cohort id definition. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getCohortDefinitionExpression
                        (Deprecated) Get a cohort definition expression
getCohortDefinitionName
                        (Deprecated) Get a cohort definition's name
                        from WebAPI
getCohortDefinitionSql
                        Get a cohort definition's SQL from WebAPI
getCohortDefinitionsMetaData
                        Get the meta data for Cohort definitions.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getCohortGenerationInformation
                        Get generation information for Cohort id.
getCohortInclusionRulesAndCounts
                        Get cohort inclusion rules and person counts
getCohortResults        Get results for a Cohort Id.
getCohortSql            Get SQL query for Cohort definition.
getConceptSetDefinition
                        Get ConceptSet id definition.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getConceptSetDefinitionBySourceKey
                        Fetch concept set definition from WebAPI by
                        SourceKey \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getConceptSetDefinitionsMetaData
                        Get the meta data for ConceptSet definitions.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getConcepts             Get concepts \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getDefinition           Get the definition for an id of chosen category
                        in WebApi. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getDefinitionsMetadata
                        Retrieve the meta data for WebApi definitions
                        of a certain category \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getEstimationDefinition
                        Get Estimation id definition.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getEstimationDefinitionsMetaData
                        Get the meta data for Estimation definitions.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getGenerationInformation
                        Get generation information. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getIncidenceRateDefinition
                        Get IncidenceRate id definition.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getIncidenceRateDefinitionsMetaData
                        Get the meta data for IncidenceRate
                        definitions. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getIncidenceRateGenerationInformation
                        Get generation information for IncidenceRate
                        id.
getIncidenceRateResults
                        Get results for a IncidenceRate Id.
getPathwayDefinition    Get Pathway id definition. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getPathwayDefinitionsMetaData
                        Get the meta data for Pathway definitions.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getPathwayGenerationInformation
                        Get generation information for Pathway id.
getPathwayResults       Get results for a Pathway Id.
getPersonProfile        Get person profile data \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
getPredictionDefinition
                        Get Prediction id definition.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getPredictionDefinitionsMetaData
                        Get the meta data for Prediction definitions.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getPriorityVocabularyKey
                        Get Priority Vocabulary Source Key
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getResults              Get generation results \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getSourceConcepts       Get source concepts that map to standard
                        concepts \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
getWebApiVersion        Get the WebAPI version number
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
insertCohortDefinitionInPackage
                        Load a cohort definition and insert it into
                        this package \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
insertCohortDefinitionSetInPackage
                        Insert a set of cohort definitions into package
invokeCharacterizationGeneration
                        Invoke generation of Characterization id.
invokeCohortGeneration
                        Invoke generation of Cohort id.
invokeGeneration        Invoke generation. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
invokeIncidenceRateGeneration
                        Invoke generation of IncidenceRate id.
invokePathwayGeneration
                        Invoke generation of Pathway id.
isValidCharacterizationId
                        is Characterization id a valid definition in
                        the WebApi. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
isValidCohortId         is Cohort id a valid definition in the WebApi.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
isValidConceptSetId     is ConceptSet id a valid definition in the
                        WebApi. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
isValidEstimationId     is Estimation id a valid definition in the
                        WebApi. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
isValidId               Check if an id is valid. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
isValidIncidenceRateId
                        is IncidenceRate id a valid definition in the
                        WebApi. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
isValidPathwayId        is Pathway id a valid definition in the WebApi.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
isValidPredictionId     is Prediction id a valid definition in the
                        WebApi. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
isValidSourceKey        Check if source key is valid.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
postCharacterizationDefinition
                        Post Characterization definition.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
postCohortDefinition    Post Cohort definition. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
postConceptSetDefinition
                        Post ConceptSet definition. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
postDefinition          Post a definition into WebApi
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
postEstimationDefinition
                        Post Estimation definition. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
postIncidenceRateDefinition
                        Post IncidenceRate definition.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
postPathwayDefinition   Post Pathway definition. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
postPredictionDefinition
                        Post Prediction definition. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
resolveConceptSet       Resolve a concept set to the included standard
                        concept IDs \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("stable")}
setAuthHeader           Manually set the authorization http header for
                        a WebAPI baseUrl In some cases the user may
                        want to manually set the authorization header.
                        An authHeader is associated with a particular
                        baseUrl and added to to the header of all http
                        requests sent to that url by ROhdsiWebApi.
updateCohortDefinition
                        Update a Cohort definition. \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
updateConceptSetDefinition
                        Update a ConceptSet definition.
                        \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
updateDefinition        Update definition \Sexpr[results=rd,
                        stage=render]{lifecycle::badge("maturing")}
